---
title: "Source Traceability"
slug: source-traceability
type: concept
source_workitem: docs/Strategic/initiatives/20260509-forge-documentation/
tags: [traceability, sources, artifacts, audit]
related_sources: [sources/readme, sources/how-to-wiki]
related_entities: [yan-justino, protocolo-es-ai]
related_concepts: [artifact-pipeline, work-item-hierarchy, wiki-architecture]
---

## Definition

Source traceability is the property that every artifact generated by forge can be traced back to one or more source documents. It is a first-class design principle of the system, not an afterthought.

## How it works

The traceability chain is maintained at three levels:

1. **Source → Wiki page**: every ingested document gets a source summary page in `docs/wiki/sources/`, which records the original file path, ingestion date, and connections to entity and concept pages.
2. **Wiki page → Artifact**: every artifact generated by `/draft` is produced using the wiki as its knowledge layer, meaning the artifact's content is implicitly grounded in ingested sources.
3. **Artifact → Source**: the `related_sources` frontmatter field on wiki pages, and the citation convention in source pages, make the chain navigable in reverse.

The global `docs/wiki/log.md` records every ingest event, including which pages were touched and which contradictions were flagged.

**Callout conventions** (from [how-to-wiki](../sources/how-to-wiki.md)):
- `> [!contradiction]` — two sources or pages conflict; never auto-resolved; user decides.
- `> [!outdated]` — content superseded by a newer source; user updates or removes the section.
- `> [!gap]` — an artifact section has no wiki coverage; never filled from training knowledge.

These callouts are the operational surface of traceability — they make uncertainty and conflict visible rather than hiding them.

## Evidence and claims

From [readme](../sources/readme.md):
> "Every artifact traces back to a source document. Every decision can be explained."

The academic grounding for this principle is Cleland-Huang et al., "Software traceability: trends and future directions" (FOSE 2014), cited in the README as the foundation for forge's traceability promise.

The operational predecessor [protocolo-es-ai](../entities/protocolo-es-ai.md) established the practical need: without traceability, AI-generated artifacts in a real transformation program cannot be audited or explained to stakeholders.

## Connections

- [artifact-pipeline](artifact-pipeline.md) — traceability is the invariant the pipeline must maintain at every step
- [work-item-hierarchy](work-item-hierarchy.md) — each level in the hierarchy maintains its own source and artifact trail

## Open questions

- How does forge surface traceability when an artifact was influenced by sources from multiple work items (e.g., a tactical story constrained by a strategic quality attribute)?
- Is traceability enforced (the tool refuses to generate if no sources are ingested) or advisory?
